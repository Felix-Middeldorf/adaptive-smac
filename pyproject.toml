[project]
name = "adaptive-smac"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["master_utils"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"